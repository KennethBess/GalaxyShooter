# OpenSpec Integration Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add OpenSpec as a devDependency, initialize the expanded profile, and document the spec-driven workflow in CLAUDE.md.

**Architecture:** OpenSpec is installed at the monorepo root (not per-workspace) and initialized to generate the `openspec/` directory. The directory is committed to git so all contributors share the same agent instructions and slash command configuration.

**Tech Stack:** Node.js 20+, npm workspaces, OpenSpec (`@fission-ai/openspec`)

**Spec:** `docs/superpowers/specs/2026-03-13-openspec-integration-design.md`

---

## Chunk 1: Dependency, Init, and CLAUDE.md

### Task 1: Add @fission-ai/openspec devDependency

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Edit package.json to add the devDependency**

  Open `package.json` and add `"@fission-ai/openspec": "latest"` to `devDependencies`:

  ```json
  {
    "name": "galaxy-shooter",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
      "apps/*",
      "packages/*"
    ],
    "scripts": {
      "dev:client": "npm run dev --workspace @space-shmup/client",
      "dev:server": "npm run dev --workspace @space-shmup/server",
      "build": "npm run build --workspace @space-shmup/server && npm run build --workspace @space-shmup/client",
      "test": "npm run test --workspace @space-shmup/server && npm run test --workspace @space-shmup/shared",
      "lint": "biome check ."
    },
    "devDependencies": {
      "@biomejs/biome": "2.4.6",
      "@fission-ai/openspec": "latest",
      "typescript": "^5.7.3"
    }
  }
  ```

- [ ] **Step 2: Install dependencies**

  Run from the repo root:
  ```bash
  npm install
  ```
  Expected: package-lock.json updated, `node_modules/@fission-ai/openspec/` directory exists.

- [ ] **Step 3: Verify openspec CLI is available**

  ```bash
  npx openspec --version
  ```
  Expected: a version string is printed (e.g., `1.x.x`). If this fails, check that `node_modules/.bin/openspec` exists.

- [ ] **Step 4: Commit**

  ```bash
  git add package.json package-lock.json
  git commit -m "feat(tooling): add @fission-ai/openspec devDependency"
  ```

---

### Task 2: Initialize OpenSpec with expanded profile

**Files:**
- Create: `openspec/` (generated by `openspec init`)

> **Note:** `openspec config profile` presents an interactive menu. You will need to select the **expanded** option when prompted. If running in a headless or non-TTY environment (e.g., a subagent), this command may hang — run it manually in a terminal if needed.

- [ ] **Step 1: Run openspec init**

  From the repo root:
  ```bash
  npx openspec init
  ```
  Expected: `openspec/` directory is created. It should contain agent instructions and slash command definitions. The exact subdirectory structure depends on the OpenSpec version but will include a `changes/` folder.

- [ ] **Step 2: Select the expanded workflow profile**

  ```bash
  npx openspec config profile
  ```
  When the interactive menu appears, select **expanded**.

  Expected: profile is saved to the OpenSpec config. Confirm by checking for output indicating "expanded" was selected, or by inspecting any config file written inside `openspec/`.

- [ ] **Step 3: Verify openspec/ structure**

  ```bash
  ls openspec/
  ```
  Expected: at minimum a `changes/` directory exists. There may also be agent instruction files (e.g., `AGENTS.md`, `OPENSPEC.md`, or similar) depending on the version.

- [ ] **Step 4: Commit the openspec/ directory**

  ```bash
  git add openspec/
  git commit -m "feat(tooling): initialize OpenSpec with expanded profile"
  ```

---

### Task 3: Add Spec-Driven Development section to CLAUDE.md

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Append the Spec-Driven Development section to CLAUDE.md**

  Add the following to the end of `CLAUDE.md`:

  ````markdown
  ## Spec-Driven Development

  This project uses [OpenSpec](https://github.com/Fission-AI/OpenSpec) for spec-driven development. All non-trivial features must be proposed and designed before any code is written.

  ### Mandate

  Before writing any code for a non-trivial feature or change, use `/opsx:propose` to create a spec. No implementation without a spec.

  ### Core Flow

  ```
  /opsx:propose "feature idea"  →  review artifacts  →  /opsx:apply  →  /opsx:archive
  ```

  ### Commands (Expanded Profile)

  | Command | Purpose |
  |---------|---------|
  | `/opsx:propose` | Create proposal.md, specs/, design.md, tasks.md for a new change |
  | `/opsx:apply` | Implement all tasks in the current change sequentially |
  | `/opsx:archive` | Move completed change to `openspec/changes/archive/` |
  | `/opsx:new` | Start a new change (alternative to propose) |
  | `/opsx:continue` | Resume work on an in-progress change |
  | `/opsx:ff` | Fast-forward through straightforward tasks |
  | `/opsx:verify` | Verify the current change before marking complete |
  | `/opsx:sync` | Sync agent instructions with current OpenSpec version |
  | `/opsx:bulk-archive` | Archive multiple completed changes at once |
  | `/opsx:onboard` | Onboard a new contributor to the spec-driven workflow |

  ### Spec Location

  Each change lives in `openspec/changes/<feature-name>/`:
  - `proposal.md` — rationale and scope
  - `specs/` — requirements and scenarios
  - `design.md` — technical approach
  - `tasks.md` — implementation checklist

  Archived changes move to `openspec/changes/archive/YYYY-MM-DD-<feature-name>/`.

  ### Maintenance

  After upgrading `@fission-ai/openspec`, run `openspec update` from the repo root to refresh agent instructions and activate the latest slash commands.
  ````

- [ ] **Step 2: Verify the section was added correctly**

  ```bash
  tail -50 CLAUDE.md
  ```
  Expected: the `## Spec-Driven Development` section appears at the end with all subsections (Mandate, Core Flow, Commands, Spec Location, Maintenance).

- [ ] **Step 3: Commit**

  ```bash
  git add CLAUDE.md
  git commit -m "docs: add Spec-Driven Development workflow to CLAUDE.md"
  ```
